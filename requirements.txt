# Default index is PyPI (do NOT override it at the top)

fastapi==0.110.0
uvicorn[standard]==0.29.0
pydantic==2.6.4
Pillow==10.2.0

mlflow==2.11.3
dvc==3.48.4

pytest==8.1.1
requests==2.31.0
python-multipart==0.0.9
prometheus-fastapi-instrumentator==6.1.0

# PyTorch CPU wheels (add as extra source, keep at bottom)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.10.0
torchvision==0.25.0